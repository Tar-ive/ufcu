# Text style rollback — UFCU Figma

Snapshot of all 59 local text styles in `pg7R89Pl8gxvJDJKQopCiD` taken immediately before the
Inter/Wise type pass on 2026-09-18. `text-styles-before.json` holds each style's id, name, font
family, weight, size, line-height and letter-spacing as they were.

To revert: feed the JSON back through `use_figma`, matching on `id`, and restore `fontName`,
`fontSize`, `lineHeight` and `letterSpacing`. Figma version history is the other route.
